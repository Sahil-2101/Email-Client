#ifndef RETRIEVE_H
#define RETRIEVE_H
#include "main.h"

/**
 * @brief To retrieve and to read from 
 * the email that has been fetched and 
 * then to remove any unwanted lines 
 * then print the rest of the email body 
 *
 * 
 */

int retrieve_email(int sockfd, int messageNum);
#endif
